import { ShoppingBag } from 'lucide-react';
import PlaceholderPage from '../shared/components/PlaceholderPage.jsx';

export default function OrdersPage() {
  return <PlaceholderPage title="Your orders" icon={<ShoppingBag />} description="Order tracking and return actions will appear here in the next step." />;
}
