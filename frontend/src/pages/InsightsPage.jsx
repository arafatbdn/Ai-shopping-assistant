import { LayoutDashboard } from 'lucide-react';
import PlaceholderPage from '../shared/components/PlaceholderPage.jsx';

export default function InsightsPage() {
  return <PlaceholderPage title="Shopping insights" icon={<LayoutDashboard />} description="Personalized spending, category, and savings insights are coming next." />;
}
